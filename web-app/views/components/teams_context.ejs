<section  id="Overview" class="mb-4">
        
    <div class="card">
      <div class="card-header text-center py-3">
        <h5 class="mb-0 text-center">
          <strong>My Teams Overview</strong>
        </h5>
      </div>
      <div class="card-body">
        <div class="table-responsive">
            <table class="table table-hover text-nowrap">
              <thead>
                <tr>
                    <th scope="col"></th>

                    <th scope="col">Team Name</th>
                    <th scope="col">Team Size</th>
                    <th scope="col"></th>

                </tr>
              </thead>
              <tbody>
                <% if(teams) {%>
  
                  <% for(let i =0 ;  i < teams.length ; i++) {%>
                <tr>
                    <td></td>

                  <td><%=teams[i].TeamName%></td>
                  <td><%=teams[i].Size%></td>
                 <td>
              <form action="/submit" method="post">
                  <input type="hidden" name="teamid" value="<%=teams[i].Team_id%>" >
                  
                  <button
                  type="submit"
                  formaction="/Quit"
                  class="btn btn-outline-danger btn-rounded"
                  data-mdb-ripple-color="dark"
                >
            Quit

                </button>
                <button
                type="submit"
                formaction="/Memebers"
                class="btn btn-outline-info btn-rounded"
                data-mdb-ripple-color="dark"
              >
                View Memebers
              </button>
            </form>
            
            
            
            </td> 
                </tr>
                <% }%>
  

                  <% } else {%>

                    <tr>


                      <td>EMPTY></td>
                      <td>EMPTY</td>


                    </tr>
                    <% }%>


              </tbody>
            </table>
    </div>
</div>
<button data-mdb-toggle="modal"
data-mdb-target="#Teams" type="button" class="btn btn-info">New Team</button>
  </section>
